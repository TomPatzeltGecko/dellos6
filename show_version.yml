---
- name: Dell Test
  hosts: dell_switches
  gather_facts: false

  tasks:
    - name: Show Version
      ansible.netcommon.cli_command:
        command: show version
      register: result

    - name: Ausgabe
      ansible.builtin.debug:
        msg: "{{ result.stdout }}"
