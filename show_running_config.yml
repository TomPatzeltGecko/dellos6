---
- name: Show Running Config
  hosts: cisco_switches
  gather_facts: false

  tasks:
    - name: Show Running Config
      cisco.ios.ios_command:
        commands:
          - show running-config
      register: result

    - name: Ausgabe
      debug:
        var: result.stdout_lines
